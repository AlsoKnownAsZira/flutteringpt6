import 'package:flutter/material.dart';

class fakerDart extends StatelessWidget {
  const fakerDart({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: Text("faker"),
        centerTitle: true,
      ),
    );
  }
}